# 阶段二：工具与框架实践指南

## 一、基础概念

### 2.1 为什么需要框架

手写 agent 循环适合学习原理，但生产场景面临：

- 工具注册管理繁琐
- 多轮对话状态维护复杂
- 流式输出、错误重试、回调钩子需重复实现

框架解决这些问题，但会引入抽象层——**先理解原理再用框架**。

### 2.2 LangChain / LangGraph

- **LangChain**：提供 Chain、Agent、Tool、Memory 的标准抽象；适合快速拼接常见场景
- **LangGraph**：基于有向图（DAG/循环图）定义 agent 状态机；适合复杂流程控制、多 agent 协作
- 核心概念：`Runnable`（可组合单元）、`State`（图节点间传递的数据）、`Edge`（条件路由）

### 2.3 Multi-agent 框架

- **AutoGen**（微软）：多个 agent 对话协作，支持人类介入；角色有 AssistantAgent / UserProxyAgent
- **CrewAI**：以"角色扮演团队"为隐喻，定义 Crew → Agent → Task 层级；适合流水线式分工

选型建议：

|场景|推荐|
|---|---|
|单 agent + 工具|LangGraph|
|多 agent 协作|AutoGen 或 CrewAI|
|快速原型|LangChain LCEL|

### 2.4 Tool Use 与 MCP 协议

- **Tool Use**：模型输出工具调用意图 → 外部执行 → 结果回传，见阶段一
- **MCP（Model Context Protocol）**：Anthropic 提出的开放标准，统一 LLM 与外部资源（文件、数据库、API）的接口规范；server 暴露工具/资源，client（LLM）按协议调用

### 2.5 向量数据库

向量数据库存储文本的嵌入（embedding）向量，支持语义相似度检索，是 RAG 的核心组件。

|数据库|特点|适用场景|
|---|---|---|
|Chroma|本地，零配置|学习、原型|
|Pinecone|托管，高性能|生产 SaaS|
|Weaviate|开源，支持混合检索|自托管生产|
|FAISS|纯向量，无持久化|离线批量检索|

---

## 二、实践依赖准备

### 2.1 安装依赖

```bash
pip install langchain langchain-openai langgraph
pip install autogen-agentchat
pip install crewai
pip install chromadb
pip install sentence-transformers  # 本地嵌入，免费
```

### 2.2 项目结构

```
02_tooling/
├── .env
├── 01_langchain_basics.py
├── 02_langgraph_agent.py
├── 03_autogen_multi.py
├── 04_crewai_team.py
└── 05_vectordb_rag.py
```

### 2.3 配置

`.env` 文件：

```env
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...
```

---

## 三、实践详细步骤

### 步骤 1：LangChain LCEL 基础

**文件：`01_langchain_basics.py`**

```python
from langchain_openai import ChatOpenAI
from langchain_core.prompts import ChatPromptTemplate
from langchain_core.output_parsers import StrOutputParser
from langchain_core.tools import tool
from langchain.agents import create_tool_calling_agent, AgentExecutor

# --- 1. LCEL Chain：Prompt | Model | Parser ---
llm = ChatOpenAI(model="gpt-4o-mini", temperature=0.2)

prompt = ChatPromptTemplate.from_messages([
    ("system", "你是一个翻译助手，将用户输入翻译为{language}。"),
    ("human",  "{input}"),
])

chain = prompt | llm | StrOutputParser()

result = chain.invoke({"input": "Hello, how are you?", "language": "中文"})
print(result)

# --- 2. 定义工具 ---
@tool
def multiply(a: int, b: int) -> int:
    """将两个整数相乘"""
    return a * b

@tool
def get_stock_price(symbol: str) -> str:
    """获取股票价格（模拟）"""
    prices = {"AAPL": 182.5, "GOOGL": 141.3, "TSLA": 248.0}
    return str(prices.get(symbol.upper(), "未找到该股票"))

tools = [multiply, get_stock_price]

# --- 3. 创建 Agent ---
agent_prompt = ChatPromptTemplate.from_messages([
    ("system", "你是一个有用的助手，可以使用工具回答问题。"),
    ("human",  "{input}"),
    ("placeholder", "{agent_scratchpad}"),
])

agent = create_tool_calling_agent(llm, tools, agent_prompt)
executor = AgentExecutor(agent=agent, tools=tools, verbose=True)

result = executor.invoke({"input": "苹果股票现在多少钱？23 乘以 47 等于多少？"})
print(result["output"])
```

---

### 步骤 2：LangGraph 状态机 Agent

**文件：`02_langgraph_agent.py`**

```python
from typing import TypedDict, Annotated, list
import operator
from langchain_openai import ChatOpenAI
from langchain_core.messages import HumanMessage, AIMessage, BaseMessage
from langchain_core.tools import tool
from langgraph.graph import StateGraph, END
from langgraph.prebuilt import ToolNode

# 1. 定义 State 结构
class AgentState(TypedDict):
    messages: Annotated[list[BaseMessage], operator.add]

# 2. 定义工具
@tool
def search(query: str) -> str:
    """搜索互联网获取信息"""
    return f"搜索 '{query}' 的结果：这是模拟的搜索结果内容。"

@tool
def calculator(expression: str) -> str:
    """计算数学表达式"""
    try:
        return str(eval(expression))
    except Exception as e:
        return f"错误: {e}"

tools = [search, calculator]
tool_node = ToolNode(tools)

# 3. 定义模型节点
llm = ChatOpenAI(model="gpt-4o-mini", temperature=0).bind_tools(tools)

def call_model(state: AgentState):
    response = llm.invoke(state["messages"])
    return {"messages": [response]}

# 4. 定义路由：是否继续调用工具
def should_continue(state: AgentState):
    last_message = state["messages"][-1]
    if hasattr(last_message, "tool_calls") and last_message.tool_calls:
        return "tools"
    return END

# 5. 构建图
graph = StateGraph(AgentState)
graph.add_node("agent", call_model)
graph.add_node("tools", tool_node)

graph.set_entry_point("agent")
graph.add_conditional_edges("agent", should_continue)
graph.add_edge("tools", "agent")  # 工具执行完回到 agent

app = graph.compile()

# 6. 运行
result = app.invoke({
    "messages": [HumanMessage(content="2024 年世界人口大约是多少？再帮我算一下 1234 × 5678")]
})

for msg in result["messages"]:
    if hasattr(msg, "content") and msg.content:
        print(f"[{type(msg).__name__}] {msg.content}\n")
```

---

### 步骤 3：AutoGen 多 Agent 协作

**文件：`03_autogen_multi.py`**

```python
import autogen

config_list = [{"model": "gpt-4o-mini", "api_key": "your_key"}]
llm_config = {"config_list": config_list, "temperature": 0.2}

# 定义角色
assistant = autogen.AssistantAgent(
    name="代码助手",
    llm_config=llm_config,
    system_message="你是一个 Python 专家，编写高质量代码并解释实现思路。",
)

reviewer = autogen.AssistantAgent(
    name="代码审查员",
    llm_config=llm_config,
    system_message="你是严格的代码审查员，检查代码的正确性、可读性和潜在 bug，给出改进建议。",
)

# UserProxyAgent 代表用户，可以执行代码
user_proxy = autogen.UserProxyAgent(
    name="用户",
    human_input_mode="NEVER",  # 自动运行，不等待真人输入
    max_consecutive_auto_reply=3,
    is_termination_msg=lambda x: "TERMINATE" in x.get("content", ""),
    code_execution_config={"work_dir": "coding", "use_docker": False},
)

# 启动群聊
groupchat = autogen.GroupChat(
    agents=[user_proxy, assistant, reviewer],
    messages=[],
    max_round=6,
)
manager = autogen.GroupChatManager(groupchat=groupchat, llm_config=llm_config)

user_proxy.initiate_chat(
    manager,
    message="请编写一个 Python 函数，实现归并排序，并包含单元测试。",
)
```

---

### 步骤 4：CrewAI 团队协作

**文件：`04_crewai_team.py`**

```python
from crewai import Agent, Task, Crew, Process
from crewai_tools import SerperDevTool  # 需要 SERPER_API_KEY

# 可选：不用搜索工具则注释掉
# search_tool = SerperDevTool()

# 1. 定义 Agent 角色
researcher = Agent(
    role="市场研究员",
    goal="深入研究指定主题，收集准确的市场数据和趋势",
    backstory="你是一名经验丰富的市场分析师，擅长从海量信息中提炼关键洞察。",
    verbose=True,
    llm="gpt-4o-mini",
    # tools=[search_tool],
)

writer = Agent(
    role="内容撰写人",
    goal="基于研究结果撰写清晰、有说服力的报告",
    backstory="你是专业的商业写作专家，擅长将复杂数据转化为易读的报告。",
    verbose=True,
    llm="gpt-4o-mini",
)

# 2. 定义 Task
research_task = Task(
    description="研究 2024-2025 年 AI agent 市场的主要玩家、融资情况和技术趋势",
    expected_output="包含 5 个关键发现的结构化研究报告，每条发现附有数据支持",
    agent=researcher,
)

write_task = Task(
    description="基于研究报告，撰写一篇 500 字的市场摘要，面向非技术管理层",
    expected_output="500 字左右的市场摘要，使用非技术语言，包含结论和建议",
    agent=writer,
    context=[research_task],  # 依赖上一个 task 的输出
)

# 3. 组建 Crew 并运行
crew = Crew(
    agents=[researcher, writer],
    tasks=[research_task, write_task],
    process=Process.sequential,  # 顺序执行；parallel 为并行
    verbose=True,
)

result = crew.kickoff()
print("\n========== 最终报告 ==========")
print(result)
```

---

### 步骤 5：向量数据库 + RAG 基础

**文件：`05_vectordb_rag.py`**

```python
import chromadb
from chromadb.utils import embedding_functions
from openai import OpenAI

client = OpenAI()

# 1. 初始化 Chroma（本地持久化）
chroma_client = chromadb.PersistentClient(path="./chroma_db")

openai_ef = embedding_functions.OpenAIEmbeddingFunction(
    model_name="text-embedding-3-small"
)

collection = chroma_client.get_or_create_collection(
    name="knowledge_base",
    embedding_function=openai_ef,
)

# 2. 添加文档（实际场景从文件/网页加载）
documents = [
    "LangGraph 是 LangChain 的扩展，用于构建有状态的多 actor 应用程序。",
    "RAG（检索增强生成）通过检索外部知识来增强 LLM 的回答准确性。",
    "向量数据库通过余弦相似度或欧氏距离进行语义检索。",
    "AutoGen 由微软开发，支持多个 AI agent 之间的对话协作。",
    "Function calling 允许模型以结构化 JSON 格式请求执行外部函数。",
]

collection.add(
    documents=documents,
    ids=[f"doc_{i}" for i in range(len(documents))],
)
print(f"已添加 {len(documents)} 条文档")

# 3. 语义检索
def retrieve(query: str, n_results: int = 2) -> list[str]:
    results = collection.query(
        query_texts=[query],
        n_results=n_results,
    )
    return results["documents"][0]

# 4. RAG 问答
def rag_answer(question: str) -> str:
    # 检索相关文档
    context_docs = retrieve(question)
    context = "\n".join(f"- {doc}" for doc in context_docs)

    response = client.chat.completions.create(
        model="gpt-4o-mini",
        messages=[
            {"role": "system", "content": f"基于以下上下文回答问题：\n\n{context}"},
            {"role": "user",   "content": question},
        ],
        temperature=0,
    )
    return response.choices[0].message.content

# 测试
questions = [
    "LangGraph 是什么？",
    "如何提高 LLM 回答的准确性？",
    "微软有没有开发什么 agent 框架？",
]

for q in questions:
    print(f"\nQ: {q}")
    print(f"A: {rag_answer(q)}")
```

---

## 四、阶段检验清单

- [ ] 用 LangChain LCEL 组装一个 Prompt → LLM → Parser 的 Chain
- [ ] 用 LangGraph 实现包含工具调用的有状态 agent
- [ ] 用 AutoGen 让两个 agent 协作完成代码编写+审查
- [ ] 用 Chroma 实现文档入库和语义检索
- [ ] 能解释 LangChain vs LangGraph 的适用场景差异

## 五、参考资源

- [LangGraph 官方文档](https://langchain-ai.github.io/langgraph/)
- [AutoGen 文档](https://microsoft.github.io/autogen/)
- [CrewAI 文档](https://docs.crewai.com/)
- [Chroma 文档](https://docs.trychroma.com/)
- [MCP 协议规范](https://modelcontextprotocol.io/)